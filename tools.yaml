sources:
  redis-main:
    kind: redis
    address: ${REDIS_HOST}:${REDIS_PORT}
    password: ${REDIS_PASSWORD}
  falkordb-main:
    kind: redis
    address: ${REDIS_HOST}:${REDIS_PORT}
    password: ${REDIS_PASSWORD}
# Other content remains unchanged
